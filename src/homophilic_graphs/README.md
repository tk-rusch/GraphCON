Code adapted from [GRAND](https://github.com/twitter-research/graph-neural-pde).
To run the experiments, simply do:

```
python run_GNN.py --dataset <dataset_name>
```

where <dataset_name> is either `Cora`, `Citeseer` or `Pubmed`.
